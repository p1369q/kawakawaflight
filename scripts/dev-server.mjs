import { createServer } from 'node:http'; import { readFile } from 'node:fs/promises'; import { extname, join, normalize } from 'node:path';
const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.ts':'text/typescript; charset=utf-8','.css':'text/css; charset=utf-8'};
createServer(async(req,res)=>{try{const url=new URL(req.url??'/', 'http://localhost'); let p=url.pathname==='/'?'index.html':url.pathname.slice(1); p=normalize(p).replace(/^\.\.(\/|\\|$)/,''); const file=join(process.cwd(),p); const data=await readFile(file); res.writeHead(200,{'content-type':types[extname(file)]??'application/octet-stream'}); res.end(data)}catch{res.writeHead(404);res.end('not found')}}).listen(5173,'0.0.0.0',()=>console.log('かわかわふらいと dev server: http://localhost:5173'));
