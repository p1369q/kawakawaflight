import { SaveSystem } from './SaveSystem.js';
type SoundName='button'|'part'|'color'|'start'|'star'|'gold'|'result';
class AudioSystemClass{private ctx:AudioContext|null=null; soundOn=SaveSystem.load().soundOn; async resume(){if(!this.soundOn)return; try{this.ctx??=new AudioContext(); if(this.ctx.state==='suspended') await this.ctx.resume();}catch{this.ctx=null}} toggle(){this.soundOn=!this.soundOn; SaveSystem.save({soundOn:this.soundOn}); return this.soundOn} play(name:SoundName){if(!this.soundOn)return; void this.resume(); const c=this.ctx; if(!c)return; try{const o=c.createOscillator(), g=c.createGain(); const f={button:420,part:520,color:620,start:330,star:760,gold:980,result:450}[name]; o.frequency.value=f; o.type=name==='gold'?'triangle':'sine'; g.gain.setValueAtTime(.0001,c.currentTime); g.gain.exponentialRampToValueAtTime(.08,c.currentTime+.02); g.gain.exponentialRampToValueAtTime(.0001,c.currentTime+.16); o.connect(g).connect(c.destination); o.start(); o.stop(c.currentTime+.18)}catch{/* no audio */}}}
export const AudioSystem=new AudioSystemClass();
